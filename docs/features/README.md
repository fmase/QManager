# Feature Notes

Per-feature deep-dives extracted from `CLAUDE.md` to keep the always-loaded prompt small. Read these on demand when working on a specific feature.

## CGI Endpoint Reference

| Feature | CGI Script | Hook | Types | Reboot? |
|---|---|---|---|---|
| Traffic Engine (Video Optimizer + Masquerade) | `network/video_optimizer.sh` | `use-video-optimizer.ts` + `use-cdn-hostlist.ts` + `use-traffic-masquerade.ts` | `video-optimizer.ts` | No |
| NetBird VPN | `vpn/netbird.sh` | `use-netbird.ts` | inline | Yes (uninstall) |
| Config Backup | `system/config-backup/{collect,apply,apply_status,apply_cancel}.sh` | `use-config-backup.ts` + `use-config-restore.ts` | `config-backup.ts` | Deferred (dialog + banner for IMEI/profile) |
| Bandwidth Monitor | `monitoring/bandwidth.sh` | `use-bandwidth-monitor.ts` + `use-bandwidth-settings.ts` | `bandwidth-monitor.ts` | No |
| Connection Scenarios | `scenarios/{activate,list,add,edit,delete}.sh` | `use-scenario-list.ts` | inline | No |
| Scenario-to-Profile Binding | `scenarios/activate.sh` (guard) + `profiles/{deactivate,list,get,save}.sh` | `use-active-profile.ts` + `hooks/use-sim-profiles.ts` | `sim-profile.ts` (ScenarioSchedule, ProfileScenarioBinding) | No |
| SMS Inbox + Alerts | `cellular/sms.sh` + `monitoring/sms_alerts.sh` + `monitoring/sms_alert_log.sh` | `use-sms-read-state.ts` (`useSmsReadState`, `smsFingerprint`, `parseSmsTimestamp`) | `SmsMessage` (`storage:"ME"\|"SM"`) | No (boot service: `qmanager_sms_storage`) |
| APN Management | `cellular/apn.sh` (actions: save/deactivate); shared lib: `apn_mgr.sh` (v2 config I/O + COPS apply + `reapply_active_apn_slot` + `reconcile_active_apn_slot_at_boot` invoked from poller `collect_boot_data()`; `active=0` → no-op) | `use-apn-settings.ts` (`useApnSettings`, `patchCidApn`) | `apn-settings.ts` (`ApnSetting`, `CidContext`, `ApnSettingsResponse`, `ApnSaveRequest`) | No |
| Band Locking | `bands/current.sh` (per-category register query: `lte_band`/`nsa_nr5g_band`/`nr5g_band`/`nrdc_nr5g_band`) + `bands/lock.sh` (band_type: `lte`/`nsa_nr5g`/`sa_nr5g`/`nrdc_nr5g`) + `bands/failover_status.sh` + `bands/failover_toggle.sh` | `use-band-locking.ts` | `band-locking.ts` (`BandCategory`, `CurrentBands`, `BandLockResponse`); `modem-status.ts` (`hw_lte_bands`, `hw_nsa_nr5g_bands`, `hw_sa_nr5g_bands`) | No |
| Known-SIMs Database | `system/known_sims.sh` (GET + POST list/clear) | — (fetched inline in `known-sims-row.tsx`) | — | No |
| Connection Quality | `system/ping_profile.sh` (GET adds `interval_override`+`effective_interval`; POST writes profile/targets only — NOT `interval_override`) + `system/quality_thresholds.sh` (GET/POST; POST touches `qmanager_quality_reload` AND `qmanager_watchcat_reload`) | `use-ping-profile.ts` (`intervalOverride`, `effectiveInterval`) + `use-quality-thresholds.ts` (flattens `custom_ms`/`custom_pct`) | `PingProfile`, `PING_PROFILES`, `QualityPreset`, `QUALITY_PRESETS` (now incl. `custom`), `QualityThresholdsSettings` | No |
| LAN Gateway / Subnet | `network/lan_config.sh` (GET/POST) | `use-lan-config.ts` | `LanConfigStatus`, `LanConfigSaveRequest`, `LanConfigSaveResponse` (`carrier_bounce?: boolean`) | No (network reload + conditional LAN-port carrier bounce) |
| Connection Watchdog | `monitoring/watchdog.sh` (GET adds `probe_profile`, `interval_override`, `effective_interval`, `quality_thresholds` passthrough, `primary_recheck_enabled`, `primary_recheck_interval`; POST: `save_settings` with `fail_threshold`/`probe_profile`/`interval_override`/`primary_recheck_enabled`/`primary_recheck_interval` — sole writer of `interval_override`; also `dismiss_sim_swap`, `revert_sim`) | `use-watchdog-settings.ts` (`WatchdogSettings` — `fail_threshold`, `probe_profile`, `interval_override`, `effectiveInterval`, `qualityThresholds`, `primary_recheck_enabled`, `primary_recheck_interval`; dropped `latency_ceiling_ms`/`loss_ceiling_pct`) | `WatchcatState` in `modem-status.ts` (`ssr_hold` union member) | Tier 4 only (token-bucketed `reboot`) |
| SMS Forwarding | `cellular/sms_forwarding.sh` (GET; POST: `save_settings`, `clear_failures`, `send_test`) | `use-sms-forwarding.ts` (`SmsForwardingSettings`, `SmsForwardingFailure`, `SmsForwardingData`, `SmsForwardingSavePayload`) | — (inline in hook) | No |
| Tailscale VPN | `vpn/tailscale.sh` (GET; POST: `install`, `install_status`, `connect`, `disconnect`, `logout`, `start_service`, `stop_service`, `set_boot_enabled`, `set_exit_node`, `uninstall`) | `use-tailscale.ts` (`TailscaleStatus`, `TailscaleSelf`, `TailscaleTailnet`, `TailscalePeer`, `UseTailscaleReturn`) | `TailscaleStatus` (inline in hook) | No |
| Diagnostics Capture | `system/diagnostics.sh` (POST: `capture`) | `use-diagnostics.ts` (`DiagnosticsStage`, `UseDiagnosticsReturn`) | `diagnostics.ts` (`DiagnosticsCaptureResponse`) | No |
| IPA Hardware Offload | `system/ipa_offload.sh` (GET; POST: `enable`, `disable`) | `use-ipa-offload.ts` (`IpaOffloadState`, `UseIpaOffloadReturn`) | `ipa-offload.ts` (`IpaOffloadGetResponse`, `IpaOffloadPostResponse`) | Deferred (pending_reboot_required; banner via `requestRebootLater("ipa_offload")`) |
| Adaptive Polling | `system/adaptive_polling.sh` (GET/POST: `save`); heartbeat: `/tmp/qmanager_ui_active`; reload flag: `/tmp/qmanager_poller_reload`; force flag: `/tmp/qmanager_force_tier2`; tier field: `.device.poller_tier` ∈ `active`\|`idle`\|`deep`; UCI: `quecmanager.poller.*` | `use-adaptive-polling.ts` | — (inline in hook) | No |
| IP Passthrough | `network/ip_passthrough.sh` (GET; POST: `apply`); config: `/etc/qmanager/ippt_config.json`; boot cache: `.device.ippt_*` in `/tmp/qmanager_status.json`; Verizon lock; `parse_ippt_nat`/`parse_ippt_usbnet`/`parse_ippt_dhcpv4dns`/`parse_ippt_mpdn_rule` in `parse_at.sh`; trailing-comma anchor invariant | `use-ip-passthrough.ts` | `ip-passthrough.ts` (`PassthroughMode`, `UsbMode`, `DnsProxy`, `IpptNat`, `IpPassthroughSettingsResponse`, `IpPassthroughSaveRequest`) | Yes (apply-then-reboot, no separate action) |
| On-Demand Radio Details | `cellular/radio_details.sh` (GET only); lib: `ondemand_radio.sh` (`load_ondemand_cache`, `ondemand_radio_fetch`, `ondemand_dataplane_refresh`); cache: `/tmp/qmanager_ondemand.json`; RAT-gate hardening (serving-cell re-read gates mimo command; skips mimo on unknown/transitioning RAT); apply-chokepoint refresh (`apn_mgr.sh` + `qmanager_profile_apply` call `ondemand_dataplane_refresh` backgrounded); `stale: true` fallback | `use-radio-details.ts` (`useRadioDetails`, `UseRadioDetailsReturn`, 7 s mount-scoped poll) | `RadioDetails`, `RadioDetailsResponse`, `RadioDetailsErrorResponse` in `modem-status.ts` | No |

## Index

| Doc | When to read |
|---|---|
| [DPI Settings](dpi-settings.md) | Traffic Engine (unified Video Optimizer + Masquerade at `/local-network/traffic-engine`), nfqws, NFQUEUE 200, persistent nft rules, mode takeover |
| [Custom SIM Profiles](custom-sim-profiles.md) | APN/TTL/IMEI/MPDN apply pipeline, lock layering, Verizon MPDN, ICCID auto-apply, 2-col page (form card + list card), pills require per-row get.sh, flat save body, mismatch derived client-side |
| [Config Backup & Restore](config-backup-restore.md) | `.qmbackup` format, AES-GCM, async restore worker, deferred-reboot pattern |
| [Language Packs](language-packs.md) | Hybrid bundled/downloaded i18n, install/remove pipeline, publishing workflow, manifest |
| [Error Code Vocabulary](error-codes.md) | Backend `{ error, detail }` contract, `resolveErrorMessage()`, `at-commands` namespace |
| [Tower Lock Failover](tower-lock-failover.md) | Tower locking, signal failover daemon, unlock/stop semantics |
| [Antenna Alignment](antenna-alignment.md) | `/cellular/antenna-alignment`, alignment meter scoring, antenna type toggle |
| [Bandwidth Monitor](bandwidth-monitor.md) | `bridge_traffic_monitor_rm551` + `websocat:8838`, UCI config, dashboard 5-state row, security constraints |
| [Scenario-to-Profile Binding](scenario-profile-binding.md) | `.scenario` schema, canonical resolution rule, cron install/teardown, teardown ordering, `scenario_locked_by_schedule` guard, schedule UI contract |
| [SMS](sms.md) | `sms_tool` binary patches, `/dev/smd11` char-device invariant, shared lock, inbox CGI, alert library, phone-number handling, client-side read/unread (`localStorage` + fingerprint), timestamp-sort invariant (`MM/DD/YY` slice-reorder), deferred cross-page toast |
| [APN Management](apn-management.md) | Single-APN model (slot 1 only), save always applies to modem (COPS cycle), deactivate action (`active=0` carrier-default), honest "Not live" badge, `patchCidApn` optimistic CID patch, `reapply_active_apn_slot` durability (no auto-resurrect on `active=0`), COPS detach/attach cycle, IMS/SOS CID tagging + AlertDialog confirm, v2 config migration, Custom SIM Profile override gate, reapply triggers (reboot/SIM-swap/profile-deactivate) |
| [Band Locking](band-locking.md) | 4 lockable band types → AT param mapping (`lte`/`nsa_nr5g`/`sa_nr5g`/`nrdc_nr5g`), SA `nr5g_band` vs NR-DC `nrdc_nr5g_band`, NR-DC fully lockable (writes stick verbatim; no coercion), substring-match hazard + SA grep-v guard, SA⇄NR-DC swap UX (key-remount), failover revert (all four categories), `supported_bands.env` 4-field cache, `policy_band` vs `ue_capability_band`, no zero-band lock, `supported_bands_hw.env` static HW universe (force-copied), `hw_*_bands` poller fields, two-tone primary/warning UI split, `refresh_policy_band()` SIM-swap re-read + `/tmp/qmanager_refresh_policy_band` flag |
| [Known-SIMs Database](known-sims.md) | Persistent ICCID set model, `sim_db_seed_if_absent` return semantics (fresh-device suppression + migration from `last_iccid`), byte-parity requirement, lock-free duplicate tolerance, `sim_db_clear_keep` keeps current SIM, CGI `known_sims.sh` list/clear contract, `previous_iccid` shape-compat |
| [Connection Quality](connection-quality.md) | HTTP-probe daemon (curl, not ICMP), ping profile→params table (daemon-owned), probe-interval ownership (Sensitivity card = interval only; watchdog sole-writer of `interval_override`), `interval_override`+`effective_interval` GET fields, `custom` preset + `custom_ms`/`custom_pct`, `avg_latency_ms` windowed-average basis for latency events, dual reload-flag invariant (`quality_thresholds.sh` POST touches both `qmanager_quality_reload`+`qmanager_watchcat_reload`), `isDefault` absence semantics, ceil-division secs→samples, GET/POST key asymmetry (`target1` vs `target_1`) |
| [LAN Gateway / Subnet](lan-config.md) | LAN IP + prefix editor, self-severing apply pattern (response before `network reload`), carrier bounce on change (brif member derivation, dnsmasq reload, 15s watchdog, double-fork detach), `carrier_bounce` response field + variable `disconnect_window_seconds` (30/5), hand-rolled POSIX validation (no `ipcalc.sh`, no jq regex), null-byte-in-shell-source / SCP deploy quirk, WoL removal artifact, `lan_address_changed` event |
| [Connection Watchdog](connection-watchdog.md) | Dual-trigger model (reachability + quality), raw `ping_streak_fail` read (not debounced `.reachable` — eliminates double-debounce), `fail_threshold` (renamed from `max_failures`; 1–20 probes), `interval_override` single-writer rule, `effective_interval` resolution, probe-interval Select UI, shared `quality_thresholds.*` (retired `latency_ceiling_ms`/`loss_ceiling_pct`), `avg_latency_ms` windowed-average basis, Tier 1 AT+COPS re-registration, reason-aware cooldown (`finish_cooldown`: primary-recheck arm → revert-settle arm → reason-aware arm), dual reload-flag invariant, recovery-lifecycle Network Events (reuse `watchcat_recovery`/`sim_failover` types), float-comparison awk rule, STATUS_STALE_THRESHOLD, NO-SIGNAL policy, state file fields, auto-disable token bucket, SSR-aware hold (`ssr_aware`, `ssr_grace`, `ssr_hold`, `ssr_in_progress`, `ssr_hold_gate`, `last_ssr_detected`), MPSS SSR amplification problem, dmesg-evidence mechanism, grace-window fall-through, SIM-failover lifecycle (`SIM_SETTLE_SECS=90` hard-coded floor — not UCI; `finalize_sim_failover` shared finalizer for both reachability + quality success arms; `/tmp/qmanager_sim_failover` `.reason` = `"connectivity_failure"`/`"quality_degradation"`; `original_iccid` pre-swap ICCID 3×retry; `revert_settle_active` shell variable for post-revert hold; `primary_recheck_enabled`/`primary_recheck_interval` UCI keys; `primary_recheck_active` in-process variable (not in state file); `initiate_primary_recheck` blind swap + settle; `watchdog_failback` profile-apply reason; `check_revert_request` 3-branch logic; `sim_db_add` on all swap paths incl. slow-modem revert; 3×1s ICCID retry; retired `last_iccid` → `sim_db_add`; `--argjson`-vs-`--arg` jq boolean gotcha) |
| [SMS Forwarding](sms-forwarding.md) | SMS forwarding daemon (`qmanager_sms_forward`): seed-on-first-run, loop guard, 3-attempt abandon + failure file (capped 20), djb2 fingerprint internal-only (UTF-8 awk ≠ UTF-16 frontend), lifted-hook architecture (`forwarding-center.tsx` owns single poll loop), Delivery & Health card 4-state model (`active`/`issue`/`unconfigured`/`off`), `send_test` reads target from UCI not request body |
| [Tailscale VPN](tailscale-vpn.md) | Two install variants (official tarball + tiny opkg), procd init.d written by heredoc only (never a static repo file), marker-file contract, `set_exit_node` action (`tailscale set` not `tailscale up`), double-fork orphan for `tailscale up`, `--accept-routes` ban (network-bricking), `uci delete` config-file resurrection quirk, `migrate_tailscale_packages` official early-return guard, `exit_node_advertised` GET field |
| [Diagnostics & IPA Offload](diagnostics.md) | `qmanager_debug_report` daemon (last-stdout-line = artifact path contract, ~300 KB cap, self-prune-to-3, UCI redaction glob list invariant, `modem_ssr_dumps` is listings-only — never reads `.elf` binaries, `ubus` dependency for `watchdog_state`), `diagnostics.sh` CGI (rawfile embed), `ipa_offload.sh` CGI (r8125_ioss — NOT rtl8125_ioss, armed-on-reboot, stop() no-op, `pending_reboot_required: true`), pessimistic Switch, qlog.sh `tr '\n' '↵'` mojibake fix |
| [Adaptive Polling](adaptive-polling.md) | `qmanager_poller` heartbeat-based AT backoff (Active → Idle → Deep tiers), `/tmp/qmanager_ui_active` epoch heartbeat, AT-due gating (`cycle_count % (interval/POLL_INTERVAL)`), full-refresh on idle AT touch, `write_cache`/`read_ping_data` always-run invariant, force-tier2 apply chokepoints (`qmanager_profile_apply`, `apn_mgr.sh`), tower-lock Active pin, boot-seed invariant, `quecmanager.poller.*` UCI (never seeded), `isDefault` absence semantics, `.device.poller_tier` status.json field |
| [IP Passthrough](ip-passthrough.md) | Apply-then-reboot one-shot pipeline (5 AT steps: MPDN_rule + QMAPWAC + IPPT_NAT + usbnet + DHCPV4DNS), config file vs boot cache two-surface read model, `qcmd` trailing-comma anchor invariant (the regression root cause), Verizon profile lock, MPDN_rule field layout (IPPT_mode field 5, MAC field 7), DHCPV4DNS enable/disable modem value vs enabled/disabled cache normalization, no-rollback partial-apply risk |
| [On-Demand Radio Details](ondemand-radio-details.md) | L1-adjacent reads (MIMO, timing advance, CGCONTRDP, QMAP) removed from poller and fetched only while UI page is open; RAT-gate hardening (re-reads serving cell before issuing mimo command; skips mimo on unknown RAT); `ondemand_dataplane_refresh` for apply paths (no L1 reads, safe post-COPS); `/tmp/qmanager_ondemand.json` cache; `load_ondemand_cache` in poller `write_cache` preserves last-known in `status.json`; `useRadioDetails` 7 s mount-scoped poll; `stale: true` last-known fallback |
