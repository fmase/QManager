# TODO

- APN Profiles not actually applying the profile upon reboot
- APN Profiles still show Active despite overwritten by Custom SIM Profile
- Custom SIM Profile doesnt resets Connection Scenarios to Balanced (default) when deactivated
- It takes the full tier time buffer to refetch the data when switching SIMs. It should be refetched instantly and populate all data to show updated information once SIM and connection were restored.
- Make the Custom SIM Profile add an input and select dropdown combo so that users can reuse APN Profile and also let them put their own APN if they want to.
