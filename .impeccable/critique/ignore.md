# Critique ignore list — findings marked "do not re-raise".
# One finding per non-comment line; matched case-insensitively against
# a finding's rule name or snippet. See /impeccable critique.

# Designer-intended: the unauthenticated Overview is deliberately kept flat
# (no FastMile circular signal meter / hero widget). The signature meter is
# reserved for the authenticated cellular overview, not the pre-login card.
no hero widget
circular signal meter
mosaic rule
